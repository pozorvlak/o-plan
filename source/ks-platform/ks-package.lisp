;;; Defines the knowledge-source package.

;;; /\/: Unfortunately, too much code assumes it's in the OPLAN package,
;;; so we use the OPLAN package.

(in-package :oplan)

(use-package :oplan-plan-world-viewer)
